1)Index.php is the router so using the hyperlinks on the pages will redirect

2)When testing the Sessions use 'phpunit --stderr SessionsTest.php' 
  This is because the --stderr prevents the error of "Session header already sent"


   